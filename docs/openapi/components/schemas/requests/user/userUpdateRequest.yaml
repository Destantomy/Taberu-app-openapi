type: object
properties:
  username:
    type: string
    example: stagiritis
  password:
    type: string
    format: password
    description: plain password will be hashed by the server
    example: secret12345